<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
  <% end %>
</ul>

<p class="btn pt-3 text-decoration-none" id="add_btn"><%= link_to "Add new restaurant", new_restaurant_path %></p>